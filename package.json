{
  "name": "vpn-master-backend",
  "version": "1.0.0",
  "description": "Control-plane API for VPN Master (WireGuard). Server catalog, per-device session/peer negotiation, tunnel-IP allocation, and monthly RX+TX quota ledger for the VPNMasteriOS client.",
  "main": "server.js",
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "db:migrate": "node db/migrate.js",
    "db:init": "node db/migrate.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}
